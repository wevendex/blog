# Animation Assets

Add Lottie JSON animations or PNG sequences used for card effects.

Example suggestions:

* `dealing.json` – Card dealing animation overlay.
* `win.json` – Fireworks or confetti when the player wins.